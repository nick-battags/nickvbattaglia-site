---
interface Props {
  title: string;
  description: string;
  href?: string;
  tags?: string[];
  external?: boolean;
}

const { title, description, href, tags = [], external = false } = Astro.props;
const Wrapper = href ? 'a' : 'div';
const wrapperProps = href
  ? {
      href,
      target: external ? '_blank' : undefined,
      rel: external ? 'noopener' : undefined,
      class: 'card group block no-underline',
    }
  : { class: 'card group block' };
---

<Wrapper {...wrapperProps}>
  <div class="flex items-start justify-between gap-4 mb-3">
    <h3 class="font-serif text-lg font-semibold text-text group-hover:text-accent transition-colors">
      {title}
    </h3>
    {href && (
      <span class="text-muted text-lg mt-0.5 group-hover:text-accent transition-colors">
        {external ? '↗' : '→'}
      </span>
    )}
  </div>
  <p class="text-sm text-text-dim leading-relaxed mb-4">{description}</p>
  {tags.length > 0 && (
    <div class="flex flex-wrap gap-1.5">
      {tags.map(tag => (
        <span class="pill">{tag}</span>
      ))}
    </div>
  )}
</Wrapper>
